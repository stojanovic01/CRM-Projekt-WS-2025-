# ============================================
# CRM-Anwendung - Konfigurationstemplate
# ============================================
# WICHTIG: Kopieren Sie diese Datei zu .env und füllen Sie die Werte!
# Die .env Datei wird von git ignoriert und enthält sensible Daten
#
# Lokal: 
#   cp .env.example .env
# PythonAnywhere:
#   nano .env (nach dem Clone)
# ============================================

# ============ Umgebung ============
# Optionen: development, production
FLASK_ENV=production

# ============ Sicherheit ============
# Erstellen Sie einen neuen zufälligen Secret Key!
# Unter Windows PowerShell: 
#   python -c "import secrets; print(secrets.token_hex(32))"
# Unter Linux/Mac:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-random-value-in-production

# ============ Datenbank (SQLite lokal) ============
# Lokal: sqlite:///instance/crm.db
# PythonAnywhere: sqlite:///app.db  (im /home/username/mysite/)
SQLALCHEMY_DATABASE_URI=sqlite:///instance/crm.db

# ============ Datenbank (MySQL - optional) ============
# Wenn Sie MySQL statt SQLite verwenden möchten:
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://user:password@localhost:3306/crm_db
# 
# PythonAnywhere MySQL:
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://username:password@username.mysql.pythonanywhere-services.com:3306/username$crm_db

# ============ Zeitzone ============
# Für österreichische Zeit:
TIMEZONE=Europe/Vienna

# ============ Debug (NUR für Entwicklung!) ============
# Lokal: True
# Production: False
DEBUG=False

# ============ Log Level ============
# Optionen: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============ Session Konfiguration ============
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# ============ Datenbank-Optionen ============
# SQLAlchemy Echo (zeige SQL-Statements) - nur für Debugging
SQLALCHEMY_ECHO=False

# Maximale Verbindungen im Pool
SQLALCHEMY_ENGINE_OPTIONS_POOL_SIZE=5
SQLALCHEMY_ENGINE_OPTIONS_MAX_OVERFLOW=10
